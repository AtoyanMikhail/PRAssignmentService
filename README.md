# PRAssignmentService

Сервис для автоматического назначения ревьюеров на Pull Request'ы с HTTPS API.

## Возможности

- 🔐 **HTTPS API** - Защищенное REST API с TLS
- 🤖 **Автоматическое назначение** - Умное распределение ревьюеров по нагрузке
- 👥 **Управление командами** - Создание и управление командами разработчиков
- ⚡ **Массовая деактивация** - Деактивация команды с переназначением PR < 100мс
- 📊 **Статистика** - Отслеживание нагрузки ревьюеров
- 🔄 **Переназначение** - Автоматическое переназначение при деактивации пользователей
- 📝 **OpenAPI** - Генерация API кода из спецификации

## Архитектура

- **HTTP Layer** - Gin framework с OpenAPI code generation (oapi-codegen)
- **Service Layer** - Бизнес-логика с транзакциями
- **Repository Layer** - Абстракция работы с БД
- **Database** - PostgreSQL с миграциями (golang-migrate) и sqlc
- **Logging** - Структурированные логи с uber/zap
- **Testing** - Unit тесты с testify/mock

## Требования

- Go 1.23+
- PostgreSQL 16+
- Docker и Docker Compose (для разработки)
- OpenSSL (для генерации TLS сертификатов)

## Быстрый старт

### Автоматическая настройка (рекомендуется)

```bash
git clone <repository-url>
cd PRassignmetService
make setup
```

Эта команда выполнит:
- Установку зависимостей
- Запуск Docker контейнеров
- Генерацию TLS сертификатов
- Применение миграций

После завершения запустите приложение:

```bash
make run
```

### Ручная настройка

### 1. Клонирование и установка зависимостей

```bash
git clone <repository-url>
cd PRassignmetService
make install
```

### 2. Генерация TLS сертификатов

```bash
make certs
```

### 3. Запуск базы данных

```bash
make docker-up
```

Это запустит:
- PostgreSQL на порту 5432
- Автоматически применит миграции

### 4. Запуск приложения

```bash
make run        # Собрать и запустить
# или
make dev        # Запустить без сборки (для разработки)
```

Сервер будет доступен на `https://localhost:8443`

**См. [docs/API_USAGE.md](docs/API_USAGE.md) для примеров использования API**

## Makefile команды

```bash
make help           # Показать все доступные команды
make install        # Установить зависимости
make build          # Собрать приложение
make run            # Собрать и запустить
make dev            # Запустить в режиме разработки
make test           # Запустить тесты
make test-cover     # Тесты с coverage
make lint           # Запустить линтеры
make clean          # Очистить собранные файлы

# Docker
make docker-up      # Запустить контейнеры
make docker-down    # Остановить контейнеры
make docker-logs    # Показать логи

# Миграции
make migrate-up     # Применить миграции
make migrate-down   # Откатить миграцию
make migrate-create # Создать новую миграцию

# Генерация кода
make sqlc           # Сгенерировать код из SQL
make generate-api   # Сгенерировать API из OpenAPI спецификации

# Прочее
make certs          # Сгенерировать TLS сертификаты
make setup          # Полная настройка проекта
```

## Конфигурация

Все параметры настраиваются через переменные окружения. См. файл `.env` для примера.

### Основные параметры:

- `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME` - настройки БД
- `SERVER_HOST`, `SERVER_PORT` - адрес сервера
- `SERVER_TLS_ENABLED` - включить/выключить HTTPS
- `SERVER_TLS_CERT_FILE`, `SERVER_TLS_KEY_FILE` - путь к TLS сертификатам
- `LOG_LEVEL`, `LOG_FORMAT` - настройки логирования

## Структура проекта

```
├── cmd/api/           # Точка входа приложения
├── internal/
│   ├── api/           # HTTP handlers (Gin + OpenAPI)
│   ├── config/        # Конфигурация
│   ├── db/            # Сгенерированный код sqlc
│   ├── logger/        # Логгер (uber/zap)
│   ├── models/        # Доменные модели
│   ├── repository/    # Слой доступа к данным
│   └── service/       # Бизнес-логика
├── database/
│   ├── migrations/    # SQL миграции
│   ├── queries/       # SQL запросы для sqlc
│   └── schema.dbml    # DBML схема
├── certs/             # TLS сертификаты
├── scripts/           # Вспомогательные скрипты
└── docs/              
    ├── openapi.yaml   # OpenAPI спецификация
    └── API_USAGE.md   # Примеры использования API
```

## Архитектура

### Слои приложения

1. **HTTP Layer (internal/api/)**
   - Gin framework для HTTP сервера
   - OpenAPI code generation с oapi-codegen
   - Автоматическая генерация типов и роутов из спецификации
   - Middleware для логирования и восстановления

2. **Service Layer (internal/service/)**
   - Бизнес-логика с транзакциями
   - Валидация данных
   - Координация между репозиториями

3. **Repository Layer (internal/repository/)**
   - Абстракция работы с БД
   - Type-safe SQL с sqlc
   - Connection pooling с pgx/v5

4. **Database Layer**
   - PostgreSQL 16
   - Миграции с golang-migrate
   - Индексы для производительности

## Разработка

### Миграции

Миграции применяются автоматически при запуске `docker-compose up`.

Для ручного управления:

```bash
# Применить миграции
migrate -path database/migrations -database "postgres://postgres:postgres@localhost:5432/pr_assignment?sslmode=disable" up

# Откатить последнюю миграцию
migrate -path database/migrations -database "postgres://postgres:postgres@localhost:5432/pr_assignment?sslmode=disable" down 1
```

### Генерация кода sqlc

```bash
sqlc generate
```
  